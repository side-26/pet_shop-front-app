interface:
  display_name: "Pet Shop UI Components"
  short_description: "Build and compose accessible RTL Pet Shop UI"
  default_prompt: "Use $shadcn-pet-ui-component to build or compose Pet Shop UI from the shared component inventory."
policy:
  allow_implicit_invocation: true
