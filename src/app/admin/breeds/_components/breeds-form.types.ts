export type BreedPetTypeOption = {
  value: string;
  label: string;
  mainImage?: string;
  thumbnail?: string;
};

export type BreedCountryOption = {
  value: string;
  label: string;
  logo: string;
};
